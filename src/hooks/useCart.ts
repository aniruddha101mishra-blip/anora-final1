/**
 * useCart — thin re-export from CartContext.
 * All cart state now lives in a single React context provider.
 * Components import from here just like before — no changes needed in consumers.
 */
export { useCart } from '@/context/CartContext'
